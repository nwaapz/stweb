# راهنمای مدیریت کارکنان (اعضای تیم)

این راهنما نحوه افزودن، ویرایش و حذف کارکنان در سیستم مدیریت صفحه درباره ما را توضیح می‌دهد.

## دسترسی به بخش مدیریت کارکنان

1. وارد پنل مدیریت شوید: `http://your-site.com/backend/admin/`
2. از منوی سمت راست، روی "صفحه درباره ما" کلیک کنید
3. روی تب "اعضای تیم" کلیک کنید

## افزودن کارمند جدید

### مراحل:

1. روی دکمه **"افزودن عضو جدید"** در بالای صفحه کلیک کنید
2. فرم را پر کنید:
   - **نام**: نام کامل کارمند (اجباری)
   - **سمت**: عنوان شغلی (اجباری)
   - **توضیحات**: توضیحات اضافی درباره کارمند (اختیاری)
   - **تصویر**: تصویر کارمند (اجباری برای افزودن جدید)
   - **ترتیب نمایش**: عدد برای تعیین ترتیب نمایش (0 به معنای اول)
   - **وضعیت**: فعال/غیرفعال
3. روی دکمه **"ذخیره"** کلیک کنید

### نکات مهم:
- تصویر باید در فرمت‌های JPEG, PNG, GIF یا WebP باشد
- حداکثر حجم فایل: 5 مگابایت
- تصویر به صورت خودکار در مسیر `backend/uploads/about/team/` ذخیره می‌شود

## ویرایش کارمند موجود

### مراحل:

1. در لیست کارکنان، روی آیکون **مداد (✏️)** کنار کارمند مورد نظر کلیک کنید
2. اطلاعات را ویرایش کنید
3. برای تغییر تصویر:
   - فایل جدید را انتخاب کنید
   - تصویر قبلی به صورت خودکار حذف می‌شود
4. روی دکمه **"ذخیره"** کلیک کنید

## حذف کارمند

### مراحل:

1. در لیست کارکنان، روی آیکون **سطل زباله (🗑️)** کنار کارمند مورد نظر کلیک کنید
2. در پنجره تأیید، روی **"OK"** کلیک کنید
3. کارمند و تصویر مرتبط با آن به صورت کامل حذف می‌شوند

### نکته مهم:
- با حذف کارمند، تصویر او نیز از سرور حذف می‌شود
- این عمل قابل بازگشت نیست

## فیلدهای فرم

| فیلد | نوع | اجباری | توضیحات |
|------|-----|--------|---------|
| نام | متن | ✓ | نام کامل کارمند |
| سمت | متن | ✓ | عنوان شغلی (مثلاً: مدیر عامل، مدیر مالی) |
| توضیحات | متن چندخطی | ✗ | توضیحات اضافی درباره کارمند |
| تصویر | فایل | ✓ (فقط برای افزودن) | تصویر کارمند |
| ترتیب نمایش | عدد | ✗ | برای تعیین ترتیب نمایش (0 = اول) |
| وضعیت | چک‌باکس | ✗ | فعال/غیرفعال بودن در نمایش |

## مثال استفاده

### افزودن کارمند جدید:

```
نام: مایکل روسو
سمت: مدیر عامل
توضیحات: با بیش از 15 سال تجربه در صنعت خودرو
تصویر: [انتخاب فایل]
ترتیب نمایش: 1
وضعیت: ✓ فعال
```

### ویرایش کارمند:

```
نام: مایکل روسو → مایکل جان روسو
سمت: مدیر عامل → مدیر عامل اجرایی
توضیحات: [ویرایش توضیحات]
تصویر: [انتخاب تصویر جدید - اختیاری]
```

## نمایش در فرانت‌اند

کارکنان از طریق API در دسترس هستند:

```javascript
fetch('/backend/api/about.php')
    .then(response => response.json())
    .then(data => {
        // data.team شامل لیست کارکنان است
        data.team.forEach(member => {
            console.log(member.name);        // نام
            console.log(member.position);    // سمت
            console.log(member.description); // توضیحات
            console.log(member.image_url);   // آدرس تصویر
        });
    });
```

## ساختار داده در API

```json
{
    "success": true,
    "team": [
        {
            "id": 1,
            "name": "مایکل روسو",
            "position": "مدیر عامل",
            "description": "با بیش از 15 سال تجربه...",
            "image_url": "http://site.com/backend/uploads/about/team/image.jpg",
            "sort_order": 1
        }
    ]
}
```

## نکات امنیتی

1. فقط مدیران سیستم می‌توانند کارکنان را مدیریت کنند
2. تصاویر آپلود شده به صورت خودکار نام‌گذاری می‌شوند
3. فایل‌های حذف شده از سرور نیز پاک می‌شوند
4. فرمت و حجم فایل‌ها قبل از آپلود بررسی می‌شود

## عیب‌یابی

### مشکل: تصویر آپلود نمی‌شود
- بررسی کنید که فرمت فایل مجاز باشد (JPEG, PNG, GIF, WebP)
- بررسی کنید که حجم فایل کمتر از 5 مگابایت باشد
- بررسی کنید که پوشه `backend/uploads/about/team/` قابل نوشتن باشد

### مشکل: کارمند حذف نمی‌شود
- بررسی کنید که به عنوان مدیر وارد شده باشید
- بررسی کنید که کارمند در دیتابیس وجود داشته باشد

### مشکل: تصویر نمایش داده نمی‌شود
- بررسی کنید که مسیر `UPLOAD_URL` در `config/database.php` صحیح باشد
- بررسی کنید که فایل تصویر در سرور وجود داشته باشد

## پشتیبانی

برای سوالات و مشکلات، با تیم توسعه تماس بگیرید.

